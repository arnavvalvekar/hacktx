{
  "name": "ecofin-carbon",
  "version": "1.0.0",
  "description": "EcoFin Carbon - Sustainable Finance Platform with Carbon Footprint Tracking",
  "private": true,
  "workspaces": [
    "src",
    "server"
  ],
  "scripts": {
    "dev": "npx concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "cd src && npm run dev",
    "dev:server": "cd server && npm run dev",
    "build": "npm run build:client && npm run build:server",
    "build:client": "cd src && npm run build",
    "build:server": "cd server && npm run build",
    "start": "cd server && npm start",
    "install:all": "npm install && cd src && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "vite": "^6.4.0"
  },
  "keywords": [
    "carbon-footprint",
    "sustainable-finance",
    "capital-one",
    "nessie-api",
    "auth0",
    "mongodb"
  ],
  "author": "EcoFin Carbon Team",
  "license": "MIT",
  "dependencies": {
    "@auth0/nextjs-auth0": "^4.11.0",
    "mongodb": "^6.20.0"
  }
}
